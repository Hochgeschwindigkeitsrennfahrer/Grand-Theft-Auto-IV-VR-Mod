# Beispiel dxvk.conf — neben GTAIV.exe legen, wenn VR-DXVK-Builds getestet werden.
# Async braucht einen async-fähigen Build (sd805-Fork / gplasync-Patches).

dxvk.enableAsync = true
d3d9.maxFrameLatency = 1
d3d9.presentInterval = 1
d3d9.numBackBuffers = 3
