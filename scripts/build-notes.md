# Build notes → canonical: docs/BUILD.md

See **[docs/BUILD.md](../docs/BUILD.md)**.

Brief:

```powershell
.\scripts\init-submodules.ps1
```

Then walk through the x86 build of `d3d9.dll` with the Cursor agent.
