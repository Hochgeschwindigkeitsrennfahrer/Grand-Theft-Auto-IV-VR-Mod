# src/

GTA-specific glue will live here (sigscan, camera, logging, OpenXR helper **outside** or beside DXVK).

v0.1: empty on purpose — first milestone is getting the **DXVK submodule** to produce a loadable Win32 `d3d9.dll`.

See `docs/ARCHITECTURE.md`.
